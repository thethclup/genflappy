# GenFlappy 🐦⛓️

**GenFlappy** is an AI-powered blockchain game running on the **GenLayer** network. It puts a Web3/Validator spin on the classic Flappy Bird concept by combining real-time gameplay with LLM-driven Intelligent Contracts (GenVM).

In GenFlappy, you play as a "Validator Bird" navigating through consensus node "pipes." Every action, from the theme of the flight to the final score evaluation, is narrated and judged dynamically by AI running directly on-chain!

## 🌟 Key Features

* **AI-Generated Game Themes:** When you start a game, the GenLayer smart contract triggers an LLM to generate a unique, dramatic flight theme based on blockchain concepts (e.g., *Zero-Knowledge Proofs*, *MEV Mempools*, *Gas Wars*).
* **Mid-Flight AI Verdicts (Checkpoints):** When you request a boost, an on-chain AI judge decides your fate. Are you struggling below a certain score? You might get a penalty. Flying high? You get a boost. The AI provides a reason for its decision in real-time.
* **Intelligent Flight Reports:** Hitting a pipe triggers the final evaluation. The smart contract writes a poetic, epic flight report of your demise and assigns a verdict (`LEGENDARY`, `EPIC`, `DECENT`, `ROOKIE`, or `DISASTROUS`).
* **On-Chain Leaderboard:** A global `TreeMap` state stores player high scores and displays the top 10 validators of the network.

## 🛠️ Tech Stack

* **Frontend:** React 18, Vite, TypeScript, Tailwind CSS
* **Blockchain/Smart Contracts:** Python (GenLayer SDK / GenVM)
* **Web3 Integration:** `genlayer-js` SDK via Browser Injection (MetaMask)
* **AI Tooling:** GenLayer's Non-Deterministic Execution (`gl.nondet.exec_prompt`)

## 📦 Project Structure

```text
├── contracts/
│   └── genflappy.py     # GenLayer Python Smart Contract with LLM prompts
├── src/
│   ├── components/      # React functional components (Canvas, Leaderboard, etc.)
│   ├── lib/             # GenLayer JS client and API interactions
│   ├── App.tsx          # Main application component
│   └── index.css        # Tailwind configurations
├── public/              # Static assets
└── package.json         # Dependencies and build scripts
```

## 🚀 Getting Started

### 1. Install Dependencies
```bash
npm install
```

### 2. Deploy the Intelligent Contract
You need to deploy the GenFlappy contract to a GenLayer testnet (e.g., Bradbury) using the GenLayer CLI.

```bash
# Setup network and account
npx genlayer network set testnet-bradbury
npx genlayer account create --name flappy-deployer
npx genlayer account use flappy-deployer

# Deploy the contract
npx genlayer deploy --contract contracts/genflappy.py
```

### 3. Environment Setup
Copy the `.env.example` file to create `.env.local` and paste the deployed contract address:

```bash
cp .env.example .env.local
```

Edit your `.env.local`:
```env
VITE_CONTRACT_ADDRESS="<YOUR_NEWLY_DEPLOYED_CONTRACT_ADDRESS>"
```

### 4. Run the Dev Server
```bash
npm run dev
```
Make sure you have a Web3 wallet (like MetaMask) installed and connected to the GenLayer Testnet to play!

## 📜 Smart Contract Highlights (`contracts/genflappy.py`)

The contract uses GenLayer's "Equivalence Principle" via `gl.vm.run_nondet_unsafe` to reach a decentralized consensus on output generated by internal LLMs. 

* `leader_fn()` queries the LLM for a creative blockchain story or judgement.
* `validator_fn(leader_result)` strictly validates the output formatting (e.g., enforcing valid JSON dictionaries with expected exact keys like `verdict`, `amount`, and `reason`).

---
*Built with ❤️ for the GenLayer Ecosystem*
